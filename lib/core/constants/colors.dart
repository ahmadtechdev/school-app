import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primary = Color(0xFF3D5EE1);
  static const Color primaryDark = Color(0xFF2E48B7);
  static const Color primaryLight = Color(0xFFD4DCFA);
  static const Color accent = Color(0xFF18AEFA);

  // Background colors
  static const Color background = Color(0xFFFFFBFA);
  static const Color card = Color(0xFFFFFFFF);
  static const Color splashBackground = Color(0xFFEEF3FF);

  // Text colors
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textHint = Color(0xFFBDBDBD);

  // Status colors
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFE53935);
  static const Color warning = Color(0xFFFFB300);
  static const Color info = Color(0xFF18AEFA);

  // Gradient colors
  static const List<Color> primaryGradient = [
    Color(0xFF3D5EE1),
    Color(0xFF18AEFA),
  ];

  // Shadow colors
  static const Color shadow = Color(0x1A000000);
}